import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});



export const metadata: Metadata = {
  title: "AI Chat Assistant - Intelligent Conversations",
  description: "Experience intelligent conversations with AI using voice and text chat.",
  keywords: ["AI chat", "voice chat", "intelligent assistant", "conversations"],
  authors: [{ name: "AI Chat Assistant Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
