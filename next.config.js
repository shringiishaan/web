/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // Enable static exports if needed
  // output: 'export',
  // trailingSlash: true,
};

module.exports = nextConfig;
